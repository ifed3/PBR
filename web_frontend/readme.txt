to host locally:

$ ruby app.rb

then navigate to localhost:4567

can also be easily deployed to heroku:

$ git init
$ heroku create
$ git push heroku master

i got an instance going on https://chihack-pbr.herokuapp.com